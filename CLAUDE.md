# 小鱼知识库问答系统 — 项目文档与协作规则

> 本文件是本项目的"总章程":包含项目需求、协作规则、技术方案、当前进度。
> 每次开始工作前必须先读本文件。

## 一、项目简介

- **项目名称**:小鱼知识库问答系统(基于 LangChain 的企业级 RAG 知识库问答系统)
- **面向场景**:电商平台商品知识库(商品介绍、参数、常见问答),用户在浏览器里提问,AI 基于知识库内容作答并展示引用了哪些片段
- **技术核心**:RAG(检索增强生成)= 先查资料、再回答,回答有据可依
- **交付形态**:本机运行的前后端网站(浏览器访问),无需部署到云服务器

## 二、与用户的协作规则(整个项目期内必须严格遵守)

1. 用户是编程小白(已在另一项目"小鱼记账"中熟悉了 Claude Code 协作模式)。**所有沟通使用中文、大白话**;必须解释技术时用生活化比喻。
2. **任何技术选型或方案调整,先列 2~3 个方案讲清对用户的影响(难度、效果、风险),由用户拍板。** 不得擅自替用户做重大决定。
3. 重大产品变更(新增功能、改动问答逻辑、改动权限设计)必须先征求用户意见。
4. 每个开发阶段完成后,用大白话向用户汇报:做了什么、怎么验证、下一步是什么。
5. 纯内部实现细节(小工具库选择等),Claude 自行决定,但要在当次汇报中用一句话说明理由。
6. 涉及密钥(阿里云 API Key、JWT 密钥):一律放 `.env` 文件(已排除在 Git 外),**绝不写进代码、绝不上传**。

## 三、项目需求清单(用户提出,验收依据)

1. ✅ 浏览器中进行知识库管理(文档上传、删除、查看状态)
2. ✅ 浏览器中与模型进行知识库问答;回答尽量引用知识库内容;**界面要显示引用了哪些知识库片段**
3. ✅ 多会话管理,每个用户有自己独立的会话列表
4. ✅ 会话记录持久化,不同时间段登录都能找回历史对话
5. ✅ 注册 / 登录 / 修改密码
6. ✅ 管理员 admin / 123456,**仅管理员**可打开知识库管理页面,普通用户只能问答
7. ✅ 应用各类性能优化,达到"企业级"效果(核心亮点,见第五章)
8. ✅ 加分功能:点赞点踩反馈、对话导出、管理员统计面板、深色主题、失败重试等

## 四、技术方案(2026-09-18 由用户选定)

| 项目 | 选择 | 说明 |
|---|---|---|
| 开发语言 | Python 3.11 | LangChain 生态的核心语言 |
| RAG 框架 | **LangChain 1.4.x**(核心框架) | 文档切分、向量库、提示词模板、流水线编排 |
| 大模型 | 通义千问 `qwen-plus`(阿里云百炼) | 性价比主力,约 1 元/百万字 |
| 向量化 | `text-embedding-v4`(1024 维) | 同账号 API,免本地部署 |
| 重排序 | `qwen3-rerank` | ⚠️ 旧版 gte-rerank-v2 已于 2026-05-30 停用 |
| 向量数据库 | Chroma(本地持久化) | 用户拍板 |
| 后端框架 | FastAPI + Uvicorn | 异步、自动接口文档、原生流式 |
| 系统数据库 | SQLite + SQLAlchemy 2.x 异步 | 用户拍板 |
| 前端 | Vite + React 18 + TypeScript + Ant Design 5 | 用户拍板 React |
| 文档解析 | PyMuPDF(PDF)、python-docx(Word)、openpyxl(Excel)、TXT/MD | 覆盖电商资料常见格式 |
| 关键词检索 | rank-bm25 + jieba 中文分词 | 混合检索用 |

**核心流水线**:问题改写 → 向量检索 + BM25 关键词检索 → RRF 融合 → qwen3-rerank 重排 → qwen-plus 流式生成(带 [1][2] 引用标注)→ 前端渲染引用卡片。

## 五、企业级优化清单(核心亮点)

| # | 优化项 | 说明 |
|---|---|---|
| 1 | SSE 流式输出 | 打字机效果,首字延迟 <1 秒 |
| 2 | 混合检索 | 向量 + BM25 双路召回 + RRF 融合 |
| 3 | 重排序 | qwen3-rerank 精选上下文 |
| 4 | 查询改写 + 历史压缩 | 多轮追问理解,超长历史自动摘要 |
| 5 | 问答缓存 | 相同问题命中缓存直接返回 |
| 6 | 异步入库队列 | 上传后台处理 + 进度条 |
| 7 | 异步全链路 | async FastAPI + aiosqlite + 异步 LLM |
| 8 | 接口限流 | slowapi 按用户限频 |
| 9 | 日志 + 全局异常 | loguru 全链路日志 |
| 10 | Token 成本统计 | 管理面板展示用量 |
| 11 | 前端优化 | React Query 缓存、会话懒加载 |
| 12 | 检索过程可视化 | 可展开查看检索片段及得分 |
| 13 | 质量评估 | 优化前后检索命中率对比数据 |

## 六、项目结构

- 项目根目录:`d:\appserv\LangChainRAG项目\`(与 AppServ 安装目录无关的独立文件夹)
- `backend/`:Python 后端(FastAPI + LangChain),虚拟环境在 `backend/venv/`
- `frontend/`:React 前端(Vite)
- `data/`:运行时数据(上传文件、向量库、数据库),**不进 Git**
- `sample_kb/`:演示用样例商品文档
- `docs/`:使用说明、测试报告、评估报告等项目文档
- `logs/`:运行日志(**不进 Git**)
- 依赖镜像:后端 pip 用清华源(`backend/venv/pip.ini`),前端 npm 用 npmmirror(`.npmrc`)
- **GitHub 仓库**:https://github.com/YHXxiaoyu/langchain-rag-kb(2026-09-19 建立,分支 `main`,署名"小鱼";已启用浏览器登录助手,推送无需输密码)

**启动方式**:
- 后端:`cd backend && venv\Scripts\python.exe run.py` → http://127.0.0.1:8000/docs
- 前端:`cd frontend && npm run dev` → http://localhost:5173

## 七、进度记录

- 2026-09-18:项目启动。完成方案调研(核实阿里云最新模型与已停用模型)、需求确认、技术选型(用户选定 Qwen + Chroma + React + SQLite)。
- 2026-09-18:**里程碑 0 完成** —— 环境与骨架:Python 3.11 虚拟环境 + 全部后端依赖(LangChain 1.4.1 / Chroma 1.5.9 / FastAPI 0.141)、React 前端工程、配置中心(`.env`)、统一日志、健康检查接口、Git 版本管理。后端启动成功,前端页面显示环境自检结果。
- 2026-09-18:**里程碑 1 完成** —— 用户系统:五张数据表、bcrypt 密码加密、JWT 令牌(修复了时区导致的"令牌签发于未来"问题)、注册/登录/改密接口、admin 自动创建、前端登录页与主框架、前后端双重权限守卫。冒烟测试 12 项全部通过。
- 2026-09-18:**里程碑 2 完成** —— 知识库管理:五种格式解析器(PDF/Word/Excel/CSV/TXT/MD)、中文友好切分、阿里云向量化入库、后台串行处理队列(带进度条)、文档删除(三处数据同步清理)、失败重试、管理页界面。样例商品文档 5 份已生成(sample_kb/)。实测:5 份文档 → 74 张知识卡片全部入库成功,检索准确,删除后数据完全还原。
- 2026-09-18:**里程碑 3 完成** —— 问答核心:混合检索器(向量 12 条 + BM25 关键词 12 条 → RRF 融合 → qwen3-rerank 精排 4 条)、多轮对话上下文、SSE 流式生成([1][2] 引用标注)、引用溯源(编号映射回原文片段)、会话管理接口(增删改查 + 历史消息)、前端聊天界面(会话列表/打字机效果/引用折叠卡片/点赞点踩/停止生成)。实测:电池容量、保修年限、退货运费三组问题回答准确且引用正确;多轮追问"它保修多久?"能正确理解指代;token 用量统计正常。
- 2026-09-18:**里程碑 4 完成** —— 多轮增强与体验优化:①查询改写(用 LLM 把"它的屏幕多大?"这类依赖上下文的问题补全成独立问题再检索,实测三轮模糊追问全部答对);②历史上下文管理(轮数 + 总字数双重限制的滑动窗口策略);③对话导出为 Markdown(含引用来源清单);④深色/浅色主题切换(CSS 变量方案,全部界面适配);⑤检索过程可视化(界面展示"系统理解为:xxx")。
- 2026-09-18:**里程碑 5 完成** —— 企业级优化冲刺:①问答缓存(TTL + LRU,相同问题秒回,实测 3.49 秒 → 0.06 秒,知识库变动自动失效);②接口限流(slowapi 按用户限频,实测拦截生效);③管理员数据看板(用户/会话/提问/token 消耗/7 天趋势图/缓存命中率/好评率);④检索过程可视化(引用卡片显示"语义检索第 N 名/关键词检索第 N 名");⑤**质量评估实验**(10 道测试题对比三种方案,报告存于 `docs/检索质量评估报告.md`)。

## 八、待办与依赖用户的事项

- ✅ **阿里云百炼 API Key 已配置**(2026-09-18 用户提供,存于 `backend/.env`,已在 `.gitignore` 排除)。**实测结果:qwen-plus 对话、text-embedding-v4 向量化(1024 维)、qwen3-rerank 重排序三个服务全部连通。**
- ⏳ 用户如有真实商品资料(说明书/参数表/FAQ)可提供给项目组,否则使用生成的模拟商品文档。

## 九、环境要点(排错参考)

- 后端启动:`cd backend && venv\Scripts\python.exe run.py`(端口 8000)
- 前端启动:`cd frontend && npm run dev`(端口 5173,已配 /api 代理转发到后端)
- 环境自检:`cd backend && venv\Scripts\python.exe tools\check_env.py`(密钥、模型连通、数据目录一次查完)
- 重排序接口实测可用地址:`https://dashscope.aliyuncs.com/api/v1/services/rerank/text-rerank/text-rerank`(百炼原生风格,非 OpenAI 兼容风格)
- 日志文件:`logs/app_日期.log`(UTF-8,按天切分,保留 30 天)
